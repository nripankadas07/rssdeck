[build-system]
  requires = ["setuptools>=68"]
  build-backend = "setuptools.build_meta"

  [project]
  name = "rssdeck"
  version = "0.1.0"
  description = "No-account RSS and YouTube feed dashboard generator."
  readme = "README.md"
  requires-python = ">=3.10"
  license = "MIT"
  authors = [{ name = "Nripanka Das" }]
  keywords = [
    "rss",
"youtube",
"feed-reader",
"privacy",
"python"
  ]
  classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Utilities"
  ]
  dependencies = [

  ]

  [project.optional-dependencies]
  dev = ["pytest>=8", "build>=1.2", "ruff>=0.6"]

  [project.scripts]
  rssdeck = "rssdeck.cli:main"

  [project.urls]
  Homepage = "https://github.com/nripankadas07/rssdeck"
  Repository = "https://github.com/nripankadas07/rssdeck"
  Issues = "https://github.com/nripankadas07/rssdeck/issues"

  [tool.pytest.ini_options]
  testpaths = ["tests"]

  [tool.ruff]
  line-length = 100
  target-version = "py310"

  [tool.setuptools.packages.find]
  where = ["src"]
